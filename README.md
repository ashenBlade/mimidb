# mimidb

## Описание

Особенности:

1. MVCC с использованием CSN (аналог оракловского SCN):
   - Меньше contention в отличие от снапшота в pg
2. UNDO лог
   - Не нужен VACUUM
   - Поэтому операции SELECT и т.д. будут быстрее
   - Храним дельту изменений (применяем, чтобы восстановить версию старую)
3. Logical ID для кортежей. Отдельный mapping layer - logical id -> physical id (page+idx)
   - Лучше поддержка индексов
   - Хранится как отдельная структура (файл), поэтому файл основных данных может быть любым, т.е. обычный heap, а можно кластеризованный индекс (выбор за пользователем)

## Цели первой версии

Самая тупая СУБД, не для прода:

1. Хранения на диске нет. Пока только в памяти все.
2. SQL и планировщика нет. В первых версиях все буду делать руками.
3. Только crud: SELECT, INSERT, UPDATE, DELETE. Без сложных подзапросов и т.д.
4. Системный каталог только в памяти.

Главная задача - разобраться с ядром: MVCC и конкурентность.

## Шаги

Этапы реализации, чтобы не было больно и не распылялся:

1. Рефакторинг инфры
   [x] Обработчик запросов оформить получше:
       [x] Отслеживать конец подключения
       [x] Разобраться с тем, как буду представлять транзакции/воркеры и т.д. - пох сейчас
   [x] Пересмотреть структуру проекта:
       [x] Заголовочные файлы хранить рядом с исходниками или также как и в pg (XXX: посмотреть mysql)
       [x] transam - это transaction access method. На самом деле это скорее storage уровень. Надо переименовать (в storage это все перенести)
       [x] Глобальный класс статический Config для конфигурации
       [x] cluster_state/worker_state в отдельные файлы вынести
       [x] BitUtils переделать под статический класс
       [x] Oid и другие сущности в другое место перенести
       [x] Переименовать IRMgrUndoRecord в IRMgrUndoRecord и WAL также
       [x] логгер сделать глобальный
   [x] Поддержка выражений:
       [x] AND , OR
       [x] <>=
       [x] func() (это объединил с операторами - тоже самое по сути)
   [x] Узлы плана сделать нормальные, а не как сейчас
   [x] парсер SQL hyrise взять1
   [x] Неявные транзакции добавить поддержку (в сам sql)
2. Поддержка конкурентности:
   [x] Множество стейтментов в запросе (сейчас только 1)
   [x] Конкурентные запросы блокаются при конфликте (при READ COMMITTED пока кидаю исключение - не могу перепроверить)
   [x] Запрос должен применять UNDO, чтобы видеть нужную версию
3. Инфраструктура:
   - Буфер пул
   - WAL и UNDO кэшируют записи в памяти, а потом сбрасывают
   - Клиента нормально обернуть:
     - Аборт транзакций
     - Отслеживание состояния
   - Спинлоки как в пг сделать (следить чтобы в дедлок или типа того не вошел)
4. Поддержка индексов
   - Реализовать обычный B+tree (пока как в ПГ сделать)
   - B+tree как индекс и возможность создавать разные индексы (пока обойдусь статическим)
5. Конкурентность:
   - READ COMMITTED реализовать
6. Большая БДшность:
   - Полноценная схема БД (для нее отдельный движок скорее всего сделаю)
   - Контрольник
   - Экзекютор (+ планировщик немного) допилить, чтобы не стыдно было
7. Отказоустойчивость:
   - Чек-суммы страниц
   - Восстановление
   - Чек-поинты
